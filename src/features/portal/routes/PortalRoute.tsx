import { AppShell } from '../../shared/components/AppShell';

export const PortalRoute = () => {
  return (
    <AppShell title="Customer portal" description="Customer-facing foundation route.">
      <section className="surface-card">
        <p>The customer portal feature area is scaffolded as part of the shared application foundation.</p>
      </section>
    </AppShell>
  );
};
