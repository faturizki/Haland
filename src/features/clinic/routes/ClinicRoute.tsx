import { AppShell } from '../../shared/components/AppShell';

export const ClinicRoute = () => {
  return (
    <AppShell title="Clinic" description="Shared clinic workspace foundation.">
      <section className="surface-card">
        <p>The clinic feature area is reserved for the production modules that will be added against the SRS.</p>
      </section>
    </AppShell>
  );
};
